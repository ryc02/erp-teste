Frontend legado preservado para referência histórica.

O runtime atual do Venner usa:
- `frontend/index.html`
- `frontend/app.js`
- `frontend/core/*`
- `frontend/modules/*`

Arquivos em `frontend/legacy/` pertencem à arquitetura anterior e não devem ser usados
como base para novas telas ou correções no shell atual.
